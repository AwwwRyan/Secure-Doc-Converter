# The LibreOffice-WASM engine (~221 MB) is NOT committed — too large for the
# repo and a free static deploy (ADR-012). `pnpm vendor:libreoffice` downloads
# it here from the ZetaOffice CDN so it can be served same-origin.
*
!.gitignore
!README.md
